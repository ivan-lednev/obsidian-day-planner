import { describe, test } from "vitest";

describe("Task collecting", () => {
  test.todo("Tasks do not contain duplicates");

  test.todo("Collects active clocks");

  test.todo("Combines tasks from daily notes with tasks from other files");
});
