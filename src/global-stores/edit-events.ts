import { createWatchable } from "../util/create-watchable";

// todo: we might not need these any more
export const editConfirmation = createWatchable();

export const editCancellation = createWatchable();
